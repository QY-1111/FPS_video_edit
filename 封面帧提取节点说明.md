# 按参数提取视频封面帧（0秒）

这是一个 ComfyUI 条件分支节点。它读取上游模板判断参数中的 `是否取封面帧`，并决定是否从输入视频中提取第 0 秒画面。

## 输入与输出

- `decision_json`：连接上游模板判断节点的 JSON/STRING 输出。
- `video`：连接 ComfyUI 原生 `Load Video` 的 `VIDEO` 输出。
- `封面帧`：单张 `IMAGE`，可连接 `Preview Image`、`Save Image` 或后续图像处理节点。

## 判断规则

- `是否取封面帧` 为 `是`：提取视频第 0 秒最接近的实际画面帧。
- `是否取封面帧` 为 `否`：静默停止该分支，后续节点不会执行，也不会生成占位图。
- 字段缺失、JSON 无法解析或值不是“是/否”时：节点给出明确错误，防止误提取。

同时兼容 `true/false`、`yes/no`、`1/0`，以及字段名 `是否提取封面帧`、`take_cover_frame`、`extract_cover_frame`。

## 连接方式

```text
上游模板判断参数 STRING ──→ decision_json
                                 按参数提取视频封面帧（0秒） ──→ IMAGE
Load Video.VIDEO ─────────→ video
```

## 输入示例

```json
{
  "视频类型": "口播",
  "输出类型": "文字模版",
  "抽帧方式": "单帧",
  "关键帧时间": [
    "00:00:01"
  ],
  "是否应用文字模板": "是",
  "是否取封面帧": "是"
}
```

注意：本节点固定提取第 `0` 秒，不使用 `关键帧时间` 中的时间。关键帧时间仍由原有“按模型文本抽取视频关键帧”节点处理。
