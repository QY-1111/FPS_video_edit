# 无意义帧过滤

这个 ComfyUI 节点用于过滤抽帧结果中的：

- 纯黑、纯白或几乎没有内容的单色帧；
- 镜头高速甩动产生的横向灯光拖影；
- 缺少有效画面结构的严重模糊帧。

节点名称：`无意义帧过滤`  
节点位置：`视频 / 关键帧 / 无意义帧过滤`

## 连接方法

```text
抽帧节点 IMAGE → 无意义帧过滤 → 有效图片 → 相似帧合并 / Preview Image / Save Image
                              └→ 无意义图片 → Preview Image（检查过滤效果）
```

建议先查看一次“无意义图片”输出。如果误删较多，将“过滤强度”改为`保守`；漏删较多则改为`强力`。

## 推荐参数

针对演唱会、舞台灯光和人物视频，先使用默认参数：

- 过滤强度：`均衡(推荐)`
- 过滤纯黑白帧：开启
- 过滤灯光拖影：开启
- 过滤严重模糊：开启
- 暗场亮度阈值：`0.07`
- 亮场亮度阈值：`0.93`
- 纯色标准差阈值：`0.018`
- 横向拖影阈值：`0.34`
- 最低主体细节密度：`0.28`
- 主体遮罩最小占比：`0.025`
- 分析分辨率：`384`

## 主体遮罩（可选）

节点本身不下载模型，因此不连接遮罩也能直接运行；此时根据画面亮度、结构和拖影方向过滤。

如果工作流中已有 YOLO、Florence 或人物分割节点，可以把人物/主体 `MASK` 连接到“主体遮罩”。遮罩面积超过“主体遮罩最小占比”时，该帧会被优先保护，能显著减少演唱者较小或背景较暗时的误删。

遮罩需要和图片一一对应。只有一张遮罩时，节点会自动应用到整批图片。

## 调节思路

- 标红的灯光拖影仍被保留：先选择`强力`；还不够时降低“横向拖影阈值”。
- 正常人物帧被误删：选择`保守`，或者连接人物遮罩。
- 黑场漏删：适当提高“暗场亮度阈值”。
- 模糊帧漏删：适当提高“最低主体细节密度”。
- 带文字的舞台画面：清晰文字会形成多方向边缘，默认规则通常会保留；重要流程仍建议检查“无意义图片”输出。

## 输出

- `有效图片`：可以继续进入相似帧合并、预览或保存节点。
- `无意义图片`：被过滤的帧，方便人工复核。
- `保留索引JSON`：原始批次中被保留的索引。
- `过滤索引JSON`：原始批次中被过滤的索引。
- `逐帧分析报告`：每帧的结论、原因和指标，方便调阈值。
- `过滤数量`：本批次被过滤的总数。

节点仅依赖 ComfyUI 自带的 PyTorch，不需要额外安装 OpenCV、YOLO 或其他 Python 包。
