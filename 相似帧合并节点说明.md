# 相似帧合并并保留最清晰帧

这个节点接收一个按视频时间顺序排列的 ComfyUI `IMAGE` 批次，将连续且高度相似的图片合并成组，并在每组中保留最清晰的一张。还可以接收一张可选的封面帧，让封面与图像集一起参与合并。

## 连接方式

```text
抽帧节点 IMAGE → 相似帧合并并保留最清晰帧 → 筛选后图片 → Preview Image / Save Image
封面帧节点 IMAGE → 相似帧合并并保留最清晰帧的「封面帧」（可选）
```

节点位于：`视频 / 关键帧 / 相似帧合并并保留最清晰帧`。

接入封面帧后，节点会把封面放在图像集最前面：如果封面与开头抽帧相似，它们会进入同一组并保留其中最清晰的一张；如果不相似，封面会作为独立组保留。封面尺寸与图像集不一致时会自动对齐。未接入封面帧时，节点行为与原来完全一致。

## 推荐起始参数

- 判定核心：`严格混合(三项都满足)`
- SSIM 阈值：`0.94`
- pHash 距离阈值：`8`
- 变化面积阈值：`0.08`
- 单像素变化阈值：`0.06`
- 连续帧比较方式：`同时比较上一帧和组首帧`
- 相似度检测区域：`全图`
- 清晰度检测区域：人物用 `中心80%`，录屏用 `全图`
- Tenengrad 权重：`0.65`
- Laplacian 权重：`0.35`
- 同分选择：`中间帧`
- 分析分辨率：`512`

## 参数含义

### 相似帧判定

- `严格混合`：SSIM、pHash、变化面积必须全部通过，最不容易误合并。
- `宽松混合`：三个指标通过任意两个即可，更容易合并人物嘴型、眨眼和鼠标移动等小变化。
- `仅SSIM`：只看结构相似度。
- `仅pHash`：只看整体视觉哈希，速度快但较粗略。
- `SSIM阈值`：越高越严格。可在 `0.92～0.98` 范围内调节。
- `pHash距离阈值`：越小越严格。常用范围 `4～12`。
- `变化面积阈值`：允许发生明显变化的画面面积比例。字幕、弹窗等局部变化可由此保护。
- `单像素变化阈值`：一个像素的灰度差超过此值，才计入变化面积，可过滤编码噪声。

### 防止渐变串组

`同时比较上一帧和组首帧` 是最稳妥的方式。当前帧必须同时接近上一帧和本组第一帧，避免细小变化逐帧累积后把差别很大的画面放进同一组。

### 清晰帧选择

- `Tenengrad` 更擅长评价有效边缘，对运动模糊较实用。
- `Laplacian` 对细节和失焦敏感，但也可能把噪点当作细节。
- 两个权重都设为 `0` 时没有清晰度依据，将按“同分选择”决定代表帧。
- 各项会先在当前组内归一化，再计算综合分，因此分数用于同组排序，不建议跨组比较。

## 区域选择建议

- 人物口播：清晰度检测用 `中心80%`，减少背景干扰。
- 软件录屏：相似度和清晰度检测都用 `全图`。
- 不关心字幕变化：相似度检测用 `上部80%(忽略底部字幕)`。
- 必须保护字幕变化：相似度检测用 `全图`；必要时降低“变化面积阈值”。

## 输出

- `筛选后图片`：每个相似组的最清晰代表帧组成的新 IMAGE 批次。
- `保留索引JSON`：例如 `[1, 4, 8]`，索引从 0 开始。接入封面帧时，封面索引为 `0`，原图像集索引整体后移一位。
- `分组与评分报告`：包含分组成员、相似度指标、各帧清晰度和最终选择。
- `分组数量`：筛选后保留的图片数量。

该实现只依赖 ComfyUI 自带的 PyTorch，不需要另外安装 OpenCV、scikit-image 或 OCR 模型。
